[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  # Node 22 ships a native WebSocket, which supabase-js requires at runtime.
  NODE_VERSION = "22"

# Local `netlify dev`: run Vite (5173) and proxy it on 8888 alongside functions.
[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  framework = "#custom"
  publish = "dist"
  # CSP is no longer in _headers (it's injected as a build-time <meta>), so even
  # if a stale dist/_headers exists it won't block Vite. Keep `dist` clean.

[functions]
  node_bundler = "esbuild"

# SPA fallback — client-side routing
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# NOTE: Security headers (CSP, HSTS, etc.) live in `public/_headers` so they
# apply to the DEPLOYED build only and don't break Vite's dev server under
# `netlify dev`. See public/_headers.
